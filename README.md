# Ludo Royale

A multiplayer browser Ludo game.

## Files
All frontend files live in the repository root.

## Render
Build command: `npm install`
Start command: `npm start`

The server listens on Render's `PORT` and uses WebSocket for live multiplayer state.
